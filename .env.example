# API
NODE_ENV=development
API_PORT=3000

# Database (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=idle
DB_PASSWORD=idle
DB_NAME=idle_mmo

# Redis (provisioned; not yet used in Fase 0)
REDIS_HOST=localhost
REDIS_PORT=6379

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
